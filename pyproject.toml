[project]
name = "hawkshot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.11.1",
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "marimo>=0.23.14",
    "pre-commit>=4.6.0",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]
